# Вкладывание в проект Villa Jaconda Loyalty App

Спасибо за интерес к нашему проекту! 🙏 Ниже приведены инструкции по вкладыванию.

## 📋 Кодекс поведения

В нашем проекте мы придерживаемся принципов:
- ✅ **Дружелюбность** - будьте вежливы со всеми
- ✅ **Открытость** - принимаем новые идеи
- ✅ **Взаимоуважение** - ценим мнение каждого
- ✅ **Конструктив** - критикуем код, а не людей

## 🚀 Процесс вкладывания

### 1. Форкнуть репозиторий
```bash
# Перейти на GitHub и нажать "Fork"
# Затем клонировать свою копию
git clone https://github.com/YOUR_USERNAME/Loyalty_app.git
cd Loyalty_app
```

### 2. Создать ветку для вашей функции
```bash
# Используйте описательные названия веток
git checkout -b feature/amazing-feature
# или
git checkout -b fix/bug-description
# или
git checkout -b docs/improve-readme
```

**Соглашение о названиях веток:**
- `feature/` - новая функция
- `fix/` - исправление ошибки
- `docs/` - обновление документации
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов
- `style/` - форматирование кода

### 3. Внести изменения

```bash
# Внесите изменения в файлы
npm run lint  # Проверьте код
npm start     # Протестируйте приложение
```

### 4. Коммитить изменения

```bash
# Используйте описательные сообщения коммитов
git add .
git commit -m "feat: добавлена аутентификация через JWT"
```

**Формат сообщений коммитов:**
```
<type>: <subject>

<body>

<footer>
```

**Типы:**
- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - обновление документации
- `style:` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` - переписывание кода без изменения функциональности
- `test:` - добавление или обновление тестов
- `chore:` - обновления зависимостей, конфигурационные файлы и т.д.

**Примеры:**
```
feat: добавлена поддержка темного режима

Реализована полная поддержка темного режима приложения.
Добавлены новые цвета в theme.js и обновлены все компоненты.

Fixes #123
```

### 5. Загрузить ветку

```bash
git push origin feature/amazing-feature
```

### 6. Создать Pull Request

1. Перейдите в свой форк на GitHub
2. Нажмите "New Pull Request"
3. Выберите вашу ветку
4. Заполните шаблон PR (см. ниже)
5. Подождите rецензии

## 📝 Шаблон Pull Request

```markdown
## Описание
Что нового / что было исправлено?

## Тип изменения
- [ ] Новая функция
- [ ] Исправление ошибки
- [ ] Обновление документации
- [ ] Рефакторинг кода

## Связанные Issues
Fixes #123

## Тестирование
Как вы тестировали эти изменения?

## Чек-лист
- [ ] Мой код следует стилю проекта
- [ ] Я провел самомотрицу кода
- [ ] Я добавил комментарии для сложного кода
- [ ] Я обновил документацию
- [ ] Я добавил тесты для новой функции
```

## 🐛 Отчёты об ошибках

### Перед созданием Issue
1. ✅ Проверьте [существующие Issues](https://github.com/nekopunk44/Loyalty_app/issues)
2. ✅ Обновите ваши зависимости: `npm install`
3. ✅ Очистите кеш: `npm cache clean --force` && `rm -rf node_modules`

### Как создать хороший отчёт об ошибке
```markdown
## Описание ошибки
Краткое описание того, что произошло.

## Шаги повторения
1. Запустите приложение
2. Перейдите на экран X
3. Нажмите кнопку Y
4. Произойдет ошибка Z

## Ожидаемое поведение
Что должно было произойти.

## Фактическое поведение
Что произошло на самом деле.

## Скриншоты
[Приложить скриншоты/видео]

## Инфо об окружении
- **ОС**: Windows 11 / macOS 12 / Linux Ubuntu 22.04
- **Node.js**: 16.0.0
- **npm**: 7.0.0
- **Версия приложения**: 1.0.0

## Логи ошибок
\`\`\`
[Вставьте erro logs]
\`\`\`
```

## ✨ Запросы на функции

Нам нравятся новые идеи! Перед тем как создавать Feature Request:

1. Проверьте [Board](https://github.com/nekopunk44/Loyalty_app/projects) - возможно уже в плане
2. Найдите похожие requests

### Как создать хороший Feature Request
```markdown
## Описание
Краткое описание желаемой функции.

## Проблема
Какую проблему это решит?

## Решение
Как это должно работать?

## Примеры
Ссылки на похожие функции в других приложениях.

## Дополнитльные контекст
Любая другая информация.
```

## 📚 Соглашения о коде

### JavaScript/React Native

```javascript
// ✅ ХОРОШО
const BookingService = {
  createBooking: async (userId, propertyId, dates) => {
    validateInput(userId, propertyId, dates);
    const booking = await database.insert('bookings', {
      userId,
      propertyId,
      startDate: dates.start,
      endDate: dates.end,
      status: 'pending'
    });
    trackEvent('booking_created', { bookingId: booking.id });
    return booking;
  }
};

// ❌ ПЛОХО
const service = {
  create: (u, p, d) => {
    let b = db.i('bookings', { u, p, s: 'pending' });
    return b;
  }
};
```

**Правила:**
- Используйте `const` по умолчанию, `let` когда нужно переприсваивать
- Давайте описывающие имена переменным
- Используйте async/await вместо callbacks
- Добавляйте JSDoc комментарии для функций
- Максимум 80 символов на строку

### Структура компонента

```javascript
import React, { useContext } from 'react';
import { View, Text } from 'react-native';
import { ThemeContext } from '../context/ThemeContext';

/**
 * BookingCard - компонент для отображения карточки бронирования
 * @param {Object} booking - объект бронирования
 * @param {Function} onPress - callback при клике
 */
const BookingCard = ({ booking, onPress }) => {
  const { theme } = useContext(ThemeContext);

  return (
    <View style={[styles.container, { backgroundColor: theme.bg }]}>
      <Text style={[styles.title, { color: theme.text }]}>
        {booking.property.name}
      </Text>
    </View>
  );
};

export default BookingCard;
```

## 📖 Документация

При добавлении новой функции:
- 📝 Обновите README.md
- 📝 Добавьте комментарии в код
- 📝 Создайте/обновите документацию в `/docs`
- 📝 Добавьте примеры использования

## 🔍 Процесс review

1. **Автоматические проверки**
   - Code style (ESLint)
   - Build status
   - Tests

2. **Ручная проверка**
   - Корректность кода
   - Соответствие стилю проекта
   - Документация

3. **Одобрение**
   - Минимум 1 одобрение от maintainer
   - Все checks должны пройти
   - Ветка должна быть в курсе с main

## 📞 Вопросы?

- 💬 GitHub Discussions: https://github.com/nekopunk44/Loyalty_app/discussions
- 📧 Email: vladbredihin4@gmail.com
- 🐛 Issues: https://github.com/nekopunk44/Loyalty_app/issues

---

Спасибо за вклад в Villa Jaconda Loyalty App! 🎉
